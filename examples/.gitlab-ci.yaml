stages:
  - Deploy

deploy:
  tags:
    - deploy
    - project
  stage: Deploy
  rules:
    - if: '$CI_COMMIT_TAG =~ /^([0-9]+)\.([0-9]+)\.([0-9]+)(b[0-9]+)?$/'
  script:
    - php vendor/bin/console cache:clear
